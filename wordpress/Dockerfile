ARG WORDPRESS_VERSION
FROM wordpress:${WORDPRESS_VERSION}

USER root

ARG WORDPRESS_UID
ARG WORDPRESS_GID
ARG WORDPRESS_USER
ARG WORDPRESS_GROUP

COPY ./adduser.sh /
RUN /adduser.sh && rm -rf /adduser.sh

USER ${WORDPRESS_USER}